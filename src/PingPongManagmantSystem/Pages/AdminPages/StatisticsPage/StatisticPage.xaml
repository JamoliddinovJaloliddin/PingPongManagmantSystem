<Page x:Class="PingPongManagmantSystem.Desktop.Pages.StatisticPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      
      
      
      xmlns:local="clr-namespace:PingPongManagmantSystem.Desktop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StatisticPage">

    <Border Background="#eff2f7">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Height="15">
                <Button Content="Bar Product" Click="BarProductPage" Width="100" HorizontalAlignment="Left"/>
                <Button Content="Sport Product" Margin="100 0 0 0" Click="SportProductPage" Width="100" HorizontalAlignment="Left"/>
                <ComboBox Width="200" HorizontalAlignment="Right"/>
            </Grid>

            <Grid Grid.Row="1">
                <DataGrid Style="{DynamicResource DataGridStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}"
                          ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" RowStyle="{DynamicResource DataGridRowStyle1}"
                          Grid.Row="1" x:Name="userDataGrid">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Sana" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Bar mahsuloti" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Sport mahsuloti" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Stol narxi" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Naxt" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Karta" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Vip karta" IsReadOnly="True" Width="*"/>
                        <DataGridTemplateColumn Header="Operations" IsReadOnly="True" Width="auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource gridEditdButton}">
                                            <TextBlock Text="Check"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>




        </Grid>
    </Border>
</Page>
